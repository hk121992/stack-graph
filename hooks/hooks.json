{
  "//": "D69 token-instrumentation hooks (issue #21). Plugin hooks wire native Claude Code events to the POSIX guard sg-token-hook.sh, which scope-gates (no-op without SG_* env), fails loud if node is missing, and hands off to lib/emit-usage.mjs. Commands are ${CLAUDE_PLUGIN_ROOT}-relative so a vendored plugin resolves them on install.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Task|Agent",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/sg-token-hook.sh postToolUse"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/sg-token-hook.sh subagentStop"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/sg-token-hook.sh stop"
          }
        ]
      }
    ]
  }
}
