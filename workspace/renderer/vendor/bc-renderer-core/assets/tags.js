/* Tag click affordances — round-7.4 unified surface.
 *
 * As of W3.14 (2026-05-15) inline <Ref> elements wrap a <sup class="cite-num">
 * with a real <a href="#ref-N"> anchor. The anchor navigates to the References
 * block at the page bottom, where each entry exposes the citation URL as a
 * normal <a>. No JS shim is needed for navigation.
 *
 * The file is kept (and loaded by template.ts) as a placeholder for future
 * inline tag interactions — e.g. <Skill> previews, <Risk> dialog affordances.
 */
(function () { /* no-op */ })();
